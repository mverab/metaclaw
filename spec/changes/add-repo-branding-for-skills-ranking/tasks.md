# Implementation Tasks

1. Define a compact visual direction for MetaClaw (color palette, typography style, icon style).
2. Generate 3 candidate logos with AI and select one based on readability at small size.
3. Generate 3 candidate repo banners with AI and select one that keeps install CTA area legible.
4. Generate 2 social preview images (Open Graph style) and pick one canonical image.
5. Add selected files to `assets/branding/` with stable names (`logo`, `banner`, `social-preview`).
6. Update README with a short “Who this is for” section (3 niches max, each with one concrete outcome).
7. Validate that README keeps install flow simple: GitHub install command first, verify second, local dev third.
8. Publish and run a quick post-publish check with `npx skills add mverab/metaclaw --list`.

---

**Notes**:
- Keep the scope intentionally small.
- Prefer one strong asset per slot over many variations.
